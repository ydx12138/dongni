# PC 新建文章封面必填实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 PC 管理端新建文章必须提交封面图片，并由后端拒绝空封面请求。

**Architecture:** 后端通过 `CreateArticleReq` 的 Gin 绑定标签校验封面；PC 管理端显示必填标记并在提交前阻止空封面。`UpdateArticleReq` 保持不变，因此编辑历史文章不受影响。

**Tech Stack:** Go、Gin、Vue 3、Vite。

---

### Task 1: 后端创建参数校验

**Files:**
- Modify: `models/dto/request.go`
- Create: `internal/handler/article_cover_validation_test.go`

- [ ] **Step 1: 写入失败测试**

测试 `dto.CreateArticleReq` 绑定 `{"title":"test","cover":""}` 时失败，绑定 `{"title":"test","cover":"https://example.com/cover.jpg"}` 时成功。

- [ ] **Step 2: 运行测试确认失败**

运行 `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./internal/handler -run CreateArticleRejectsMissingCover -count=1`。

预期：空封面当前可通过绑定，测试断言失败。

- [ ] **Step 3: 增加 DTO 必填标签**

将 `CreateArticleReq.Cover` 修改为：

```go
Cover string `json:"cover" binding:"required"`
```

不修改 `UpdateArticleReq.Cover`。

- [ ] **Step 4: 运行后端测试**

再次运行同一条测试命令，预期通过。

### Task 2: PC 管理端必填提示

**Files:**
- Modify: `vue6122/src/views/admin/ArticleEdit.vue`

- [ ] **Step 1: 添加封面必填标记**

将封面标签改为：

```vue
<label class="field-label">封面图片 <span class="required-mark">*</span></label>
```

添加 `.required-mark { color: var(--danger); }`。

- [ ] **Step 2: 在新建提交前校验封面**

在 `save(status)` 的标题校验后加入：

```js
if (isNew.value && !form.value.cover.trim()) {
  alert('请上传封面图片')
  return
}
```

- [ ] **Step 3: 构建前端**

在 `vue6122` 目录运行 `npm run build`，预期构建成功。

### Task 3: 全量验证

**Files:** 无新增文件。

- [ ] **Step 1: 格式化和检查差异**

运行 `gofmt -w models/dto/request.go internal/handler/article_cover_validation_test.go`，再运行 `git diff --check`。

- [ ] **Step 2: 运行后端验证**

运行 `$env:GOCACHE = (Join-Path (Get-Location) '.gocache'); go test ./... -count=1 -timeout 180s; go build ./...`。

- [ ] **Step 3: 手动验收**

新建文章未上传封面时，保存草稿与直接发布均显示“请上传封面图片”；上传封面后可正常创建；编辑历史无封面文章仍可保存。
